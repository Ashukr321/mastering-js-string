/**
 * @description: What is a string in JavaScript?
 * 
 * Strings are a primitive data type used to represent textual data.
 * In JavaScript, they are UTF-16 code unit sequences.
 */

// Creation
let single = 'single quotes';
let double = "double quotes";
let template = `backticks`; // Template literals

console.log(typeof single); // "string"
console.log(single.length); // length property
